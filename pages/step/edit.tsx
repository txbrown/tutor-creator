import { Box } from '@chakra-ui/react';
import * as React from 'react';

interface IEditStepProps {}

const EditStepPage: React.FunctionComponent<IEditStepProps> = (props) => {
  return <Box></Box>;
};

export default EditStepPage;
